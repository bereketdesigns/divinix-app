---
// This component is now purely presentational.
// All logic is handled by the client-side script.
---
<header>
  <nav class="container">
    <a href="/" class="brand">Divinix</a>
    
    <!-- Both buttons are rendered, but one is hidden via the script -->
    <a href="/login" id="create-profile-btn" class="nav-button">Create Your Profile</a>
    <a href="/edit" id="edit-profile-btn" class="nav-button" style="display: none;">Edit Profile</a>
  </nav>
</header>
<script>
  // This script runs on every page load to determine the user's login state
  // and show the correct navigation button.
  document.addEventListener('DOMContentLoaded', () => {
    const createBtn = document.getElementById('create-profile-btn') as HTMLAnchorElement | null;
    const editBtn = document.getElementById('edit-profile-btn') as HTMLAnchorElement | null;
    
    // The presence of our 'auth_token' cookie is the source of truth for login state.
    const isLoggedIn = document.cookie.includes('auth_token=');

    if (isLoggedIn && createBtn && editBtn) {
      createBtn.style.display = 'none';
      editBtn.style.display = 'inline-block';
    } else if (createBtn && editBtn) {
      createBtn.style.display = 'inline-block';
      editBtn.style.display = 'none';
    }
  });
</script>
<style>
  header {
    position: sticky;
    top: 0;
    width: 100%;
    background-color: var(--header-bg-color);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    z-index: 100;
    border-bottom: 1px solid var(--border-color);
  }
  nav {
    padding-top: 0;
    padding-bottom: 0;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .brand {
    font-size: 1.75rem;
    font-weight: 800;
    color: var(--text-color);
  }
  .nav-button {
    font-siz